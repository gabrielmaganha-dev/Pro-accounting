{
  "name": "pro-accounting",
  "version": "1.0.0",
  "private": true,
  "description": "Sistema de gestão de clientes, contratos e faturas — Pro Accounting",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=20.11.0"
  },
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev:api": "npm run dev --prefix backend",
    "dev:web": "npm run dev --prefix frontend",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "typecheck": "npm run typecheck --prefix backend && npm run typecheck --prefix frontend",
    "lint": "npm run lint --prefix backend && npm run lint --prefix frontend",
    "db:up": "docker compose up -d",
    "db:down": "docker compose down",
    "db:logs": "docker compose logs -f postgres",
    "db:migrate": "npm run db:migrate --prefix backend",
    "db:seed": "npm run db:seed --prefix backend",
    "db:seed:demo": "npm run db:seed:demo --prefix backend",
    "db:studio": "npm run db:studio --prefix backend",
    "db:reset": "npm run db:reset --prefix backend"
  }
}
